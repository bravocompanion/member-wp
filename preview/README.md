# Offline preview

The standalone preview containing locally generated demo state is distributed separately from this public repository.

For repository development, run the Next.js app with `DEMO_MODE=true`. It generates 233 synthetic taxpayer records and does not require Supabase.
